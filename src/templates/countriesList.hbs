<ul class='preview-list'>
  {{#each this}}
    <li class='preview-item'>
      <img class='preview-img' src='{{flags.svg}}' alt='flag of {{name.official}}' width='80' />
      <p class='preview-name'>{{name.official}}</p>
    </li>
  {{/each}}
</ul>